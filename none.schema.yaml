# Rime schema settings
# encoding: utf-8

schema:
  schema_id: none 
  name: "空"
  version: "1.0"
  ascii_icon:                         # string
  author:
    - cyhfvg
  description: |
    空输入方案，作为外挂dictionary依赖方案

translator:
  dictionary: extra_dict